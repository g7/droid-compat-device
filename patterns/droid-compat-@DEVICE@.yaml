# This pattern is populated automatically during build with
# the packages built by this adaptation.
#
# If you wish to add some other package, DO NOT edit this file.
# Instead, create the pattern-includes.d directory in your rpm source
# root and put the new packages there (using list members).
#
# Those includes are automatically appended to this pattern during
# the package build.

Description: Pattern with packages for @DEVICE@ compatibility layer
Summary: droid-compat @DEVICE@
Name: droid-compat-@DEVICE@
Requires:
# - pattern:jolla-hw-adaptation-@ADAPTATION_DEVICE@
# Includes will be appended here
